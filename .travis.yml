language: python
sudo: false
python:
- '2.7'
install: pip install tox-travis
script: tox
deploy:
  provider: pypi
  user: $pypi_user
  password: $pypi_password    
  on:
    tags: true
    repo: jcooklin/snap-plugin-lib-py